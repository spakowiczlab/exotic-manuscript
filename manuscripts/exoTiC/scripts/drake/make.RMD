---
title: "exo[n] project spawn: from kraken2/bracken output to shared tables"
output: html_document
---

# Date stamp
```{r setup}
knitr::opts_chunk$set(echo = TRUE)

as.character(Sys.Date())
```
# Load pipeline
```{r, message=FALSE, warning=FALSE}
abstracted.files <- list.files("R/")
lapply(abstracted.files, function(x) source(paste0("R/", x)))
```

# Run pipeline and check for failures
```{r}
make(
  plan, # defined in R/plan.R
  verbose = 2
)
loadd(paths)
# vis_drake_graph(plan, targets_only = T, file = paste0(files.dstainstamp, "_schema-of-plan.pdf"),
                # navigationButtons = F)
drake_ggraph(plan, targets_only = T, main = "exoTiC Manuscripts", label_nodes = T) 
  # ggplot2::ggsave(paste0(files.dstamp, "_schema-of-plan.pdf"), width = 25, height = 15)
```


